# Healthy Care App — 專案說明（給 Claude Code 的交接筆記）

## 這是什麼
一個第二型糖尿病預防用的生活型態管理網頁 App（PWA），繁體中文介面，給個人使用，
部署在 GitHub Pages。技術上是純前端 React App（無後端伺服器），所有資料存在使用者
瀏覽器的 localStorage，AI 拍照熱量分析則是瀏覽器直接呼叫 Google Gemini 或
Anthropic Claude 的 API（使用者自己在畫面上填 API Key，Key 只存本機瀏覽器）。

## 檔案結構
```
index.html          網頁殼層，載入 app.bundle.js，含 PWA meta tags
app.bundle.js        打包後的最終產出檔案 —— 不要手動編輯，這是編譯出來的
manifest.json        PWA 設定檔（App 圖示、名稱等）
sw.js                Service Worker，離線快取用（network-first 策略）
icon-*.png / icon.svg  App 圖示

source/App.jsx        主要畫面與狀態管理（分頁、表單、既有的 Tab 元件）
source/entry.jsx       進入點，掛載 App 到 #root，最上面 import 了 storage-shim.js
source/storage-shim.js 把 Claude.ai artifact 專用的 window.storage API
                        用瀏覽器原生 localStorage 模擬出來，讓這個獨立版本能正常運作
source/package.json    npm 依賴與 build／test 指令

source/lib/health.js   ★健康領域邏輯與參考值（BMI／體脂／骨骼肌率／腰圍分區、
                        BMR 與每日熱量、喝水量、風險評分、食物資料庫、CONTENT_REVIEW）
                        這個檔案是用 tools/extract-health.mjs 從舊版 App.jsx 機械抽取的，
                        逐字元相同。要改裡面的數值請直接改這裡，不要手打複製到別處
source/lib/goals.js    達標判定、成長階段、花園累計。含 FIELDS（資料欄位名）
source/lib/storage.js  所有 storage 存取、保留期規則、備份與還原
source/lib/foodMemory.js 同一個食物的熱量標準值（她改過的數字，下次直接用）
source/lib/useGarden.js 把達標判定與每日摘要包成 React hook 供 App.jsx 使用

source/components/Sprout.jsx      植物（八階段 × 四活力）。**這個檔案是產生出來的**，
                        由 tools/gen-sprout.mjs 從 tools/health-forest-stages.json
                        （使用者提供的設計稿）轉寫，不要手改，要改請重跑產生器
source/components/Garden.jsx      花園場景（完成的樹）
source/components/Rings.jsx       三環與圖例
source/components/GrowthPanel.jsx 主畫面的樹苗／花園切換卡
source/components/ActivityPanel.jsx 活動力畫面（三環＋本週格子）
source/components/DietDiary.jsx   以天為單位的飲食日記

source/tools/          一次性的改版工具與測試（見下）
```

## 重新打包指令（改完 source/ 之後一定要跑，app.bundle.js 才會更新）
```bash
cd source
npm install
npm run build
```
`npm run build` 就是原本那條 esbuild 指令，已經寫進 package.json。esbuild 版本鎖在
0.28.2（devDependencies），這樣每次打包的產出才可重現 —— 否則沒辦法用「重打包後檔案
有沒有變」來判斷是不是忘記打包了。

改動邏輯後跑測試：
```bash
cd source
npm test
```

本機實際跑起來看（不需要額外安裝任何東西）：
```bash
node source/tools/serve.mjs 4173
```
然後開 <http://localhost:4173/>。確認沒問題再 commit + push，GitHub Pages 會自動
重新部署。

## source/tools/ 裡是什麼
- `test-goals.mjs` — 達標／成長／花園規則的測試（`npm test` 跑的就是這個）
- `serve.mjs` — 本機靜態伺服器
- `extract-health.mjs` — 從舊版 App.jsx 抽出 lib/health.js。已經跑過，保留是為了
  留下「這些數值是機械搬移、不是手打」的證據
- `wire-modules.mjs`、`patch-*.mjs` — 這次改版的一次性修改腳本，保留當作變更紀錄，
  **不要重跑**（它們的替換目標已經不在了，重跑只會失敗）

## 樹苗養成與花園（2026-09 改版新增）
- **達標的定義**：一天要三項**全部**達成才算 —— 熱量低於 1500 大卡、
  運動 30 分鐘、喝水 2000cc。缺一項整天就不計入。三個都是固定數字，
  定義在 `lib/goals.js`（`CALORIE_CEILING`／`EXERCISE_GOAL_MIN`／`WATER_GOAL_ML`）
- **熱量判定用固定上限 1500，不是個人化目標。** 所以個人資料不完整（沒年齡、
  沒量過基礎代謝率）也判定得出來。個人化的熱量目標仍然顯示在「今日熱量」卡片上
  當參考，但**不決定這天算不算**
- **熱量達標有下限 `CALORIE_MIN_LOGGED` = 500，這個下限不能拿掉。**
  「低於 1500」對一份空的飲食紀錄（0 大卡）永遠成立，所以沒有下限的話，
  忘記記錄飲食會被判定成完美控制，花園會在沒人追蹤的日子長大。
  500 低到不會誤殺真的吃很少的一天，高到能擋掉只記了一杯咖啡或什麼都沒記
- **熱量在全 App 只有一個判定數字（1500）。** 飲食日記的當日進度條也用它 ——
  曾經日記用個人化目標、環用 1500，同一件事兩個數字會讓人搞不清到底哪個算

### 熱量標準值（同一個食物記住她改過的數字）
- **問題**：包裝食品的熱量印在標示上、永遠不變（御選肉鬆飯糰就是 251），
  但 AI 每次都從外觀重新猜一次，她每次都要重改一次。改過的數字應該算數
- **規則在 `lib/foodMemory.js`，存在 `food-calories`（新的鍵，一天一食物一列）**
- **只有「她給的數字」才會被記住，AI 自己估的絕對不記。** 這是整個功能會幫忙
  還是會默默污染日記的分界：如果把 AI 的估算也記起來，一個猜測就會變成「標準
  值」，然後被自動套用到之後每一張照片，而畫面上沒有任何地方承認它本來只是猜的。
  三個入口都只在她真的動過數字時才記：拍照卡片裡改過（跟 `aiCalories` 比對）、
  日記那行輸入框真的觸發過 onChange（`editedCaloriesRef`）、手動輸入
- **名字只做「去空白、去標點」的正規化，不做模糊比對。**「白飯」和「白飯（大碗）」
  是兩個食物。把一個份量的熱量套到另一個份量上，正是這個功能該防止的錯
- **會直接套用，但一定看得見、一定可以一鍵退回。** 卡片上寫「已直接用你之前改的
  251 大卡。AI 這次估 320 大卡。」下面就是「這次改用 AI 估的 320 大卡」。
  一個自己變掉又不說的數字，比它取代掉的那個猜測更糟
- 手動輸入那邊是**提示不是自動填**：從頭打字通常代表是同名的另一份量
- **「我的熱量標準值」卡片可以看和刪。** 記錯的數字如果看不到也刪不掉，這個功能
  就變成一個會一直給錯數字的東西。刪掉之後下次重新讓 AI 估
- 備份／還原／重設都含這份資料；還原時會過同一道 `normalizeMemory` 修補，
  因為備份檔是使用者可以編輯的純文字，壞掉的數字會直接進到日記裡
- 測試在 `tools/test-food-memory.mjs`；拍照卡片的文案可以用
  `tools/preview-analysis.jsx` 看（那張卡要有 API key 才會出現，
  預覽頁不含 App 的樣式表，是用來看**字**的）

- **成長階段**：由**累計**達標天數推進（不是連續），一棵樹 30 個達標日，
  分八階：種子 0／發芽 1／幼芽 3／小苗 6／成長 11／成熟 17／破土 24／森林之樹 30。
  斷一天不歸零，只是那天不往前走。階段名稱與順序來自使用者的設計稿，
  `lib/goals.js` 的 STAGES 和 Sprout.jsx 的圖必須一一對應（測試會擋）
- **花園**：每完成一棵樹就永久種進花園。花園狀態完全由每日摘要推算出來，
  沒有第二份資料會漂移
- **每日摘要**（`daily-summary`）：一天一筆、四個欄位，**永久保留不裁切**。
  因為飲食／喝水／運動紀錄分別只留 30／60／90 天，達標歷史必須比它們活得久
- **回填**：首次啟動時會從既有紀錄回推，但最多只能回推 30 天（判斷熱量達標要靠
  飲食紀錄）。更早的沒有資料可還原
- **植物不會枯死，而且連顏色都不變**：沒達標只有**表情**變睏，植物本身一模一樣。
  這樣「長到哪一階」和「今天過得怎樣」才是兩件分開讀得出來的事。
  絕對不要做成枯萎或轉褐 —— 這是給狀況不好的日子也會打開的 App
- **植物有臉，而且刻意跟喝水的水滴同一套語言**：`#3A4C3A` 的圓點眼睛、
  `#F4A6B7` 腮紅、`#F2C55C` 閃光、睏的時候眼睛變成閉合弧線（設計稿在種子和
  破土兩階就是這樣畫的）。四種表情剛好對應「今天達成幾項」（0／1／2／3）。
  第一版畫成植物學上正確但沒有表情的樹，結果像圖表而不像會想照顧的東西 ——
  **不要改回沒有臉的版本**。這些顏色是寫死的而不是跟主題變色，理由跟水滴永遠是
  藍色一樣：它是一個角色，不是一個介面表面
- **植物的圖是使用者的設計稿，不是我畫的。** 來源是她提供的
  「Health Forest 植物養成圖示系統」設計畫布，八張圖存在
  `source/tools/health-forest-stages.json`，由 `source/tools/gen-sprout.mjs`
  轉寫成 `components/Sprout.jsx`。**不要手改 Sprout.jsx，也不要自己重畫** ——
  要改外觀請改設計稿那份 JSON 再重跑產生器，讓 App 跟設計永遠是同一張圖。
  設計稿本身的規則：96×96 畫布（viewBox `-2 -2 100 100`）、
  所有描邊一律 `#3A4C3A` 粗 4.5、圓角端點、葉子只用同一個形狀靠旋轉縮放變化
- **產生器只做一件危險的事：把每一階固定的表情挖掉**，好讓 App 換上四種心情。
  這件事已經悄悄出錯過兩次，都不會報錯：
  ① 森林之樹的臉沒被挖掉（它畫在樹冠 y24，不像其他階畫在盆子 y79），
  結果樹冠一張臉、地上又多一張浮著的臉；
  ② 同一棵樹的**樹幹和它腳下的草丘被誤刪**，因為它們的起始高度剛好跟嘴巴同一帶，
  樹就浮在半空中。所以現在：臉的位置是**從每張圖自己讀出來的**（`FACE_AT`），
  不是全部釘在同一個高度；而且 `tools/test-sprout.mjs` 會拿設計稿逐個圖形比對，
  要求「設計稿畫的東西全都還在，只准少掉幾個落在記錄下來的臉部座標附近的小記號」。
  刪掉一根樹幹會被擋下來，留下一張臉也會
- **臉長在那個階段的主體上**：盆栽那七階在盆子上，森林之樹在樹冠上。
  這是設計稿自己的安排，`FACE_AT` 只是把它記下來。曾經做過「臉永遠在同一個位置」
  的版本，結果一棵樹底下掛著一張臉，看起來像兩隻生物而不是一隻長大
- **每一階跟地面接觸的高度不一樣**（`groundY(stage)`）：盆栽踩在盆底 y91，
  森林之樹踩在自己的草丘 y88。花園要把樹種在某個點上，用單一常數會讓樹浮起來
- **破土和森林之樹的圖裡本來就有閃光**，所以 `OWN_SPARKLES` 會擋掉 App 再加一層
- **改植物外觀時用 `source/tools/preview-sprout.jsx`**：一頁列出八階段 × 四表情，
  判斷「是不是同一隻生物在長大」只能一次看全部，一張一張截圖看不出來
- **插圖要小而精緻，不要大。** 植物被容器限高在 160px（`.growth-scene.is-sprout`），
  不是滿版。放大之後每個粗糙的地方都會被放大 —— 厚重的褐色土堆、粗線條的臉。
  現在土壤是柔和的接地陰影而非褐色色塊、五官線條變細、色塊加了細微明暗。
  花園（`.is-garden`）保留自己的長寬比，但地面用同樣的淡色處理，
  否則兩個檢視只差一次點擊卻像兩個 App
- **Sprout 與 Garden 的 svg 不寫死 inline 尺寸** —— inline style 會蓋掉所有
  stylesheet 規則，導致容器無法限制它。尺寸由放它的地方決定
- **八個階段共用同一個 viewBox**（`-2 -2 100 100`），這是設計稿的取景方式：
  每張圖自己就已經填滿畫布，所以階段推進時卡片高度不會跳動，也不會出現
  「一顆種子浮在大片空白裡」
- **三個每日條件各有一個小角色**（`components/GoalIcons.jsx`：飯碗、鞋子、水滴），
  取代單調的色點。臉就是狀態：未達標是圓點眼＋平嘴，達標變笑眼＋腮紅＋閃光。
  **角色的身體色比環的色票淺** —— 色票是圖表用色，墨色的臉壓在上面會看不見
  （`--move` 幾乎跟墨色同明度）。這跟水滴吉祥物用淺藍填色而非描邊色是同一個理由

## 頭像、稱謂與每日問候
- **頭像**：`components/AvatarPicker.jsx`。圓形、可換，跟 LINE 一樣。選到的圖會
  **中心裁切成正方形再縮到 200px**（`AVATAR_SIZE`）才存進 profile —— 它跟健康
  紀錄擠在同一個 localStorage 裡，直接塞手機原圖會有把整個儲存區撐爆、連體態
  紀錄一起壞掉的風險。實測一張直幅照片裁切後約 3 KB
- 沒設頭像時預設顯示豆苗角色，不是灰色人形剪影
- **稱謂**：`profile.nickname`，早晚的問候與總結都用它稱呼
- **每日正向語（真乘心語）／晚間總結**：`lib/coach.js` ＋
  `components/DailyCoach.jsx`。**一定要有一張卡在畫面上**：晚上 7 點前是
  「真乘心語」，7 點後換成總結。曾經做成「11 點後就不顯示、晚上 7 點才回來」，
  結果整個白天什麼都看不到 —— 使用者以為這個功能沒做。
  **一個大部分時間看不到的功能等於沒做。**
  11 點前會多一行「早安，{稱謂}」，之後只留標題。
  可以關掉，關掉的狀態記在 `coach-dismissed`
- **真乘心語的句子是使用者自己的文件，不是 App 的文案。** 存在
  `lib/quotes.js`，由 `tools/import-quotes.mjs` 從她的 .docx 產生
  （文件裡每個 `*` 是一句的開頭）。**不要改寫、不要修標點、不要套用
  coach.js 的語氣規則** —— 那些規則是用來管 App 自己寫的字。
  要更新請重跑匯入器而不是手改這個檔，讓它保持是原文的忠實副本。
  只收 12–36 字的句子（手機卡片兩行內讀完）；更長的在原文裡是段落不是格言。
  原文若有破損（括號沒閉合）會被排除並在匯入時列出來，不會私自修改她的字
- **文案有三條規則，`tools/test-coach.mjs` 會擋**：只講行為不評判人、
  不做任何醫療宣稱、**永遠先講一件做對的事再講不足**。連三項全沒達成的那天
  也要有東西可以講（那天做對的事就是「有打開來看」）。測試裡有一份禁用字
  黑名單（失敗／不夠／退步／血糖會…）
- 早晨問候由日期決定，所以**同一天整個早上都是同一句** —— 每次打開都換一句
  會變成雜訊而不是問候

## 核心功能（目前已完成，供你了解全貌）
- **個人資料**：年齡/性別/身高/體重/主要病徵；平時活動量改成「強度/中度/輕度」三種
  勾選＋每週分鐘數，App 依 WHO/ADA 準則自動換算成 sedentary/light/active 供內部計算用
- **飲食建議**：紅綠燈飲食指南；AI 拍照熱量分析（Gemini 免費／Anthropic 付費可切換）；
  手動輸入；飲食紀錄（最近7天，可直接編輯已存的熱量數字，含照片縮圖）；官方食品營養
  成分資料庫查詢連結
- **今日熱量目標**：優先用「體態紀錄」裡最新一筆的基礎代謝率(BMR)計算，沒有的話用
  年齡/性別/身高/體重公式估算；使用者也可以手動輸入自訂目標覆寫系統計算值；有展開
  區塊說明計算方式
- **喝水量**：依體重/性別/活動量估算目標；一鍵快速記錄常見份量；歷史紀錄以「每日
  總量」保存（不是每次點擊都存一筆）；可愛水滴角色依達成率變換表情；有趨勢圖
- **運動建議**：每週運動計畫模板；運動紀錄與達成率（依強度分類為有氧/阻力/柔軟度，
  跟每週150分鐘目標比較，給出建議文字）；運動項目含快走以外的多種選項
- **體態紀錄**：體重/BMI/腰圍/體脂肪/內臟脂肪/骨骼肌率/體年齡/BMR/**睡眠時數**；
  睡眠有自己的趨勢圖與參考區間（`sleepZones`，7-9 小時為建議範圍）。
  **睡眠刻意不是第四個每日達標項** —— 達標的三項是花園在數的東西，偷偷改變
  「達標」的定義會把已經記錄的每一天的意義都改掉；BMI、體脂肪率、
  骨骼肌率、腰圍的趨勢圖都有依衛福部/醫療常見標準做顏色分區背景（一眼看出好壞）；
  歷史紀錄預設只顯示最新3天，超過會有「展開全部歷史紀錄」按鈕依月份分組顯示
- **資料備份**：匯出/匯入 JSON，匯出優先跳出手機分享選單（可存 Google Drive/iCloud），
  裝置不支援分享 API 時自動退回直接下載

## 重要架構決策（改的時候要注意）
- **App 名稱是 Healthy Care（2026-09 從「糖前哨」改名）。改名只動顯示字串，
  絕對不要動 localStorage 的鍵名** —— `profile`、`body-records`、`food-log`、
  `water-log`、`exercise-log`、`daily-summary`、`food-calories` 這些是找到既有
  資料的唯一途徑，
  改了等於把手機上所有紀錄變成孤兒。改名是換標籤，不是資料遷移。
  Service Worker 的 `CACHE_NAME` 則要跟著換版，否則舊快取會讓改名看不出來
- **`lib/health.js` 已經手改過**（`sleepZones` 與對應的 CONTENT_REVIEW 條目），
  所以 `tools/extract-health.mjs` 現在會拒絕覆蓋它。要改健康數值請直接改
  `lib/health.js`，不要重跑抽取器
- **在 lib/ 新增 export 後記得加進 `export { }` 清單** —— 忘記加的話 esbuild
  會直接報 "No matching export"（這點它擋得住，跟忘記 import 不同）
- **資料欄位名不能猜。** 讀錯欄位不會報錯，只會回傳 0，然後某個判定就永遠不成立。
  這已經真的發生過一次：運動時間存的是 `durationMin`，但判定邏輯讀 `minutes`，
  結果運動這項永遠無法達標，而測試因為用了同樣錯的名字所以全過。三個欄位名集中在
  `lib/goals.js` 的 `FIELDS`，測試裡有直接斷言。新增讀取紀錄的程式碼一律用 `FIELDS`
- **`lib/health.js` 是健康數值的唯一來源。** 不要把裡面的常數或公式複製到別的檔案 ——
  兩份副本遲早會各自漂移，而漂移的那份不會報錯，只會給出錯的健康建議
- **飲食紀錄的照片會過期，文字不會。** 滿 30 天（`PHOTO_DAYS`）只刪照片、保留文字，
  這樣日記才有長期歷史。照片一張約 20KB，一年三餐就會超過瀏覽器 localStorage 的
  容量上限；文字一天約 300 bytes，十年才 1MB。**不要改成連文字一起刪**
- **iOS 的 `input[type=date]` 會無視 `width:100%`。** 它是原生控制項，Safari 依
  自己的內容決定寬度，結果「體態紀錄 → 日期」欄位整條撐出卡片外（使用者回報的
  「超線」）。桌機瀏覽器看起來完全正常，所以這種問題只在她的手機上出現。
  解法在 CSS：date／time 欄位加 `-webkit-appearance:none` 讓它照一般輸入框排版。
  另外 `.field-row` 是 grid，子項預設 `min-width:auto` 不會縮到比內容窄，
  所以也補了 `min-width:0` —— 兩欄式的列以後放寬欄位才不會又擠出去
- **不要**把資料存取改回 `window.storage` 直接呼叫官方 Claude.ai 環境的版本；這個獨立
  版本靠 `storage-shim.js` 模擬同樣的 get/set/delete/list 介面存進 localStorage，維持
  這層抽象可以讓程式碼其餘部分不用大改
- 所有 `setXxxForm({...form, field: value})` 這種直接展開更新的 pattern，如果同一個表單
  裡有 checkbox/chip 點擊跟文字輸入同時存在，容易觸發 React 批次更新的競態問題（已經修過
  一次真實案例：exerciseForm 的 chip 選擇被緊接著的文字輸入蓋掉）。新增表單欄位時優先用
  `setForm((f) => ({...f, field: value}))` 函式型寫法，比較安全
- AI 拍照分析的 prompt 裡有特別要求：如果照片拍到包裝食品的營養標示文字，要優先讀取
  標示上的數字，而不是純外觀估算；也要求 AI 誠實回報 confidence，不要灌水
- 健康相關的參考數值（BMI/體脂肪/骨骼肌率/腰圍分區、飲水量公式、熱量計算公式）都盡量
  引用衛福部/國健署等官方或準官方來源，並記錄在 `CONTENT_REVIEW` 這個常數裡（含
  `lastReviewed` 校對日期與 `sources` 來源清單），之後調整這類數值記得一併更新它
- 曾經有一個「網址參數自動記錄」的功能（給 iOS 捷徑/Apple 健康自動化用）因為有資料
  被意外覆蓋的風險而被移除了；除非使用者明確要求，不要重新加類似「打開頁面就自動
  寫入資料」的機制
- **Apple Watch／Apple 健康連動做不到，不要再評估。** HealthKit 只開放給原生 iOS
  App，網頁沒有這個 API。要做到必須整個改寫成原生 App（Mac＋Xcode＋開發者帳號年費），
  而且三項裡只有運動分鐘能自動，不值得
- **手機主動跳的推播提醒也做不到。** 純前端靜態網站沒有後端可以推送。提醒的實際做法
  是「傍晚打開 App 時，若還差一項就顯示」，搭配補登前一天（`canBackfill`）

## 部署方式
GitHub Pages：repo 設定為 Public，Settings → Pages → Build and deployment →
Source 選「Deploy from a branch」→ Branch 選 main、資料夾選 `/ (root)`。
上傳/push 新的 `app.bundle.js` 等檔案後，GitHub Pages 會在幾分鐘內自動重新部署。

## 使用者背景（互動時可以注意的地方）
使用者不是工程背景，之前在這個專案的開發過程中，Shortcuts、GitHub 網頁上傳、
Service Worker 快取等技術性步驟都需要很詳細、逐步的圖解式說明才能順利完成。
說明時盡量白話、步驟化，避免預設對方熟悉開發者工具或術語。
