# Healthy Care App — 專案說明（給 Claude Code 的交接筆記）

## 這是什麼
一個第二型糖尿病預防用的生活型態管理網頁 App（PWA），繁體中文介面，給個人使用，
部署在 GitHub Pages。技術上是純前端 React App（無後端伺服器），所有資料存在使用者
瀏覽器的 localStorage，AI 拍照熱量分析則是瀏覽器直接呼叫 Google Gemini 或
Anthropic Claude 的 API（使用者自己在畫面上填 API Key，Key 只存本機瀏覽器）。

## 檔案結構
```
index.html          網頁殼層，載入 app.bundle.js，含 PWA meta tags
app.bundle.js        打包後的最終產出檔案 —— 不要手動編輯，這是編譯出來的
manifest.json        PWA 設定檔（App 圖示、名稱等）
sw.js                Service Worker，離線快取用（network-first 策略）
icon-*.png / icon.svg  App 圖示

source/App.jsx        主要畫面與狀態管理（分頁、表單、既有的 Tab 元件）
source/entry.jsx       進入點，掛載 App 到 #root，最上面 import 了 storage-shim.js
source/storage-shim.js 把 Claude.ai artifact 專用的 window.storage API
                        用瀏覽器原生 localStorage 模擬出來，讓這個獨立版本能正常運作
source/package.json    npm 依賴與 build／test 指令

source/lib/health.js   ★健康領域邏輯與參考值（BMI／體脂／骨骼肌率／腰圍分區、
                        BMR 與每日熱量、喝水量、風險評分、食物資料庫、CONTENT_REVIEW）
                        這個檔案是用 tools/extract-health.mjs 從舊版 App.jsx 機械抽取的，
                        逐字元相同。要改裡面的數值請直接改這裡，不要手打複製到別處
source/lib/goals.js    達標判定、成長階段、花園累計。含 FIELDS（資料欄位名）
source/lib/storage.js  所有 storage 存取、保留期規則、備份與還原
source/lib/foodMemory.js 同一個食物的熱量標準值（她改過的數字，下次直接用）
source/lib/vision.js   拍照送 AI 的共用傳輸層與兩份 prompt（食物、健檢報告）
source/lib/reports.js  健檢報告的儲存格式與把關（合理範圍、只留數字不留照片）
source/lib/plan.js     把健檢報告接到每天的紀錄：每 7 天的重點、每月總分析
source/lib/workouts.js 運動建議的名單、排序與 YouTube 搜尋連結
source/lib/nutritionTags.js 食材標籤 →「增加什麼負擔／幫助什麼」的文案與個人化比對
source/lib/visits.js   就醫紀錄（日期／科別／病症／醫師建議／回診日）與回診提醒
source/lib/screening.js 建議檢查項目與科別（依報告數值＋年齡的公費篩檢）
source/lib/examPlans.js 排定的檢查（把建議加上日期，會進提醒卡）
source/lib/calendar.js 把排定的檢查做成 .ics 交給手機行事曆
source/lib/bodyScan.js 從體重計照片讀出體態數值填進表單（合理範圍把關）
source/lib/useGarden.js 把達標判定與每日摘要包成 React hook 供 App.jsx 使用

source/components/Sprout.jsx      植物（八階段 × 四活力）。**這個檔案是產生出來的**，
                        由 tools/gen-sprout.mjs 從 tools/health-forest-stages.json
                        （使用者提供的設計稿）轉寫，不要手改，要改請重跑產生器
source/components/Garden.jsx      花園場景（完成的樹）
source/components/Rings.jsx       三環與圖例
source/components/GrowthPanel.jsx 主畫面的樹苗／花園切換卡
source/components/ActivityPanel.jsx 活動力畫面（三環＋本週格子）
source/components/DietDiary.jsx   以天為單位的飲食日記
source/components/HealthAnalysis.jsx 健康分析頁（本週重點／每月分析／健檢報告）
source/components/WorkoutSuggestions.jsx 運動建議與影片連結
source/components/FoodImpact.jsx 每一餐的影響（要留意的／有幫助的）
source/components/ClinicVisits.jsx 回診提醒、建議檢查、就醫紀錄
source/components/WeeklyPlanCard.jsx 每週運動目標（七天卡片，不是表格）
source/components/ExerciseIcons.jsx 有氧／阻力／柔軟度三個小角色

source/tools/          一次性的改版工具與測試（見下）
```

## 重新打包（改完 source/ 之後一定要跑，app.bundle.js 才會更新）

只有一行要記：

```bash
cd source && npm run rebuild
```

它會跑測試 → 打包 → 告訴你產出有沒有變、多大（`tools/build-all.mjs`）。
**測試沒過就不會打包**，因為 `app.bundle.js` 是實際被部署出去的檔案，
從一份規則已經不成立的程式碼打包出來比打包失敗更糟。第一次要先 `npm install`。

本機看畫面：

```bash
cd source && npm run serve
```

然後開 <http://localhost:4173/>。確認沒問題再 commit + push，GitHub Pages 會自動
重新部署。**改版時記得換 `sw.js` 的 `CACHE_NAME`**，否則手機上的舊快取會讓改動
看不出來。

esbuild 版本鎖在 0.28.2（devDependencies），這樣每次打包的產出才可重現 ——
否則沒辦法用「重打包後檔案有沒有變」來判斷是不是忘記打包了。

**測試清單不寫在 package.json 裡，是用掃的**（`tools/run-tests.mjs` 掃
`tools/test-*.mjs`）。原本手寫清單的話，新增的測試檔要有人記得去加才會跑，
而一個沒在跑的測試比沒有測試更糟 —— 它看起來像有覆蓋到。

給「換一台電腦或很久以後回來」的自己看的版本在 `REBUILD.md`，那一頁是獨立的，
不需要先讀這一份。

## source/tools/ 裡是什麼
留下來的都是還會用到的：

- `build-all.mjs` — `npm run rebuild` 跑的就是它（測試 → 打包 → 回報）
- `run-tests.mjs` — 掃出並跑完所有 `test-*.mjs`（`npm test`）
- `check-imports.mjs` — 找出「用了但忘記 import」的名字。esbuild 抓不到這種
  （它只抓得到缺少的 named export），漏掉會變成白畫面而不是錯誤訊息
- `test-*.mjs` — 各項規則的測試
- `serve.mjs` — 本機靜態伺服器
- `gen-sprout.mjs` ＋ `health-forest-stages.json` — 從設計稿產生植物的圖
- `import-quotes.mjs` — 從真乘心語的 .docx 重新匯入句子
- `preview-sprout.jsx`／`preview-analysis.jsx` — 一次看全部狀態，用來判斷畫面

**一次性的改版腳本已經刪掉了**（`patch-*.mjs`、`wire-modules.mjs`、
`extract-health.mjs`）。它們的替換目標早就不存在，重跑只會失敗，而「當時改了什麼」
git 記錄裡就有 —— 留著它們只是讓 tools/ 看起來比實際上複雜。

## 樹苗養成與花園（2026-09 改版新增）
- **達標的定義**：一天要三項**全部**達成才算 —— 熱量低於 1500 大卡、
  運動 30 分鐘、喝水 2000cc。缺一項整天就不計入。三個都是固定數字，
  定義在 `lib/goals.js`（`CALORIE_CEILING`／`EXERCISE_GOAL_MIN`／`WATER_GOAL_ML`）
- **熱量判定用固定上限 1500，不是個人化目標。** 所以個人資料不完整（沒年齡、
  沒量過基礎代謝率）也判定得出來。個人化的熱量目標仍然顯示在「今日熱量」卡片上
  當參考，但**不決定這天算不算**
- **熱量達標有下限 `CALORIE_MIN_LOGGED` = 500，這個下限不能拿掉。**
  「低於 1500」對一份空的飲食紀錄（0 大卡）永遠成立，所以沒有下限的話，
  忘記記錄飲食會被判定成完美控制，花園會在沒人追蹤的日子長大。
  500 低到不會誤殺真的吃很少的一天，高到能擋掉只記了一杯咖啡或什麼都沒記
- **熱量在全 App 只有一個判定數字（1500）。** 飲食日記的當日進度條也用它 ——
  曾經日記用個人化目標、環用 1500，同一件事兩個數字會讓人搞不清到底哪個算

### 健檢項目（2026-09 擴充到 41 項數值 ＋ 7 項定性）
- 一份台灣健檢報告的內容遠比原本的 15 項多。現在涵蓋血糖、血脂、血壓、血液計數、
  肝功能、腎功能、甲狀腺、電解質與營養素（含維生素D、鐵蛋白）、發炎指標（hs-CRP）、
  腫瘤標記、骨密度、身體數值
- **定性項目**（糞便潛血、尿蛋白、尿糖、尿中紅血球、B肝抗原、C肝抗體、幽門桿菌）
  存在 `report.flags`，值只有「陰性」「陽性」兩種。刻意不做「弱陽／1+／2+」——
  那需要判讀，而她能從報告上直接抄下來的只有陰陽
- **「沒測」和「測了是陰性」是兩件事**，所以定性欄位有三態（未填／陰性／陽性），
  明年比對時才分得出來
- **這條保證比任何單一項目都重要：任何「會被判定區間」的項目，一定要嘛有生活型態
  規則、要嘛標記 `referral: true`。** 加了新項目卻沒有對應規則的話，它會出現在表格
  裡但完全沒有後續，讀起來就是「App 看了一眼然後沒話說」。
  `tools/test-plan.mjs` 會逐項檢查，加項目忘了接就會失敗
- **所有需要醫師判讀的異常合併成一張轉診卡**（`referralFocus`），不是一項一張。
  四十幾項的報告如果一個異常一張卡，這週真正能自己做的兩三件事會被埋掉
- 轉診說明按身體部位分（`REFERRAL_NOTES`）。**腎功能那條的存在目的就是明說
  「App 不提供蛋白質、鹽分的建議」** —— 那是醫師依整體狀況決定的
- 腫瘤標記那條要寫「高於參考值不等於罹癌，也不能單靠它排除疾病」。
  這類數字最容易讓人嚇到或誤以為安全
- **手動輸入表單的分組可以收合**，預設只展開血糖／血脂／血壓（以及照片有讀到值的
  那幾組）。41 個欄位一次全開是一面牆

### 眼睛、就醫紀錄與建議檢查（2026-09 新增）
- **眼睛的項目直接套進既有的「數值＋定性」模型**：視力左右、眼壓左右是數值，
  眼底（視網膜）檢查、白內障是定性。這樣總覽的三格計數、轉診卡、備份、
  照片辨識的 prompt 全部自動涵蓋，不用為眼睛另開一套
- **定性項目的兩個答案改成每一項自己決定**（`flag.values`）：抽血是「陰性／陽性」，
  眼科是「正常／異常」。原本寫死陰陽，「眼底檢查：陽性」根本讀不懂
- 眼睛全部標記為 `referral`：視力下降和眼壓偏高的原因都要眼科判讀，
  眼壓還得搭配視神經與視野一起看，不是 App 能解釋的
- **就醫紀錄（`clinic-visits`）是所有「請找醫師」的另一半。** 沒有它，
  轉診卡是死路。欄位：就醫日期／科別／病症／醫師建議／下次回診
- **醫師建議原封不動存、原封不動顯示。** App 沒有立場改寫醫師的話，
  也沒有任何程式去讀這個欄位做判斷。病症也是她自己的說法，不是編碼過的診斷
- 唯一會被程式使用的是「下次回診日」：**逾期的提醒永遠不會因為太久而消失**。
  三個月前錯過的回診比下週要回的更值得說，把它靜靜拿掉等於 App 替她決定那件事
  不重要了
- **建議檢查會講「掛哪一科」**（`lib/screening.js`）。「去找醫師」誠實但幫助有限，
  對一個沒在醫院裡繞過的人，講出是哪一個櫃檯才是重點。每一條都附：檢查項目、
  科別、來自她報告的理由、以及這個建議的依據（國健署或學會的指引）
- **已經看過的建議會標成「已看過」而不是消失**：一直叫她做已經做過的事，
  這份清單她就不看了；但完全拿掉會像 App 忘記了
- 年齡是選填欄位，所以沒填年齡時公費篩檢那幾條不出現（不是錯誤，是正常狀態），
  畫面上會說「填了年齡之後這裡還會多幾項」
- **建議只給檢查項目與科別，不給治療。** `tools/test-visits.mjs` 擋掉
  吃藥／劑量／手術／確診這類字眼
- **每一條建議都可以「排定時間」**（`lib/examPlans.js`，存在 `exam-plans`）。
  建議清單本來只能說「建議去做」，那是容易的一半；決定事情會不會發生的是日期，
  而原本沒有地方放日期 —— 她看過、想著要約，下個月這張卡還是講一樣的話
- 排定是**獨立的一筆紀錄**，不是建議上的欄位（建議每次都從報告重算，不存狀態），
  也不是就醫紀錄（那代表「去過了」，這代表「要去」）。兩者後來會合流：
  去過之後記一筆就醫紀錄，建議自己就安靜了
- 另外有「自己加一項檢查」，因為醫師交代的檢查不一定在清單上。
  沒有這個，選項只有「我們清單裡的」或「沒地方放」
- **建議清單預設收合**（只留標題、項數、已排幾項）。它是參考資料
- **但已經排定的檢查列在收合外面**，展開三項、超過三項才收。原因：
  排在兩個月後的檢查在提醒卡（14 天內）看不到，收合又把它藏在關起來的卡片裡，
  結果哪裡都看不到。**她真的答應要做的事，不應該需要點一下才找得到**

### 所有紀錄清單統一收合到 3 筆（2026-09）
- 飲食日記（本來 5 天）、本週運動紀錄、體態紀錄、報告紀錄、就醫紀錄、
  已排定的檢查 —— 全部都是「開三筆，其餘收合」。一致比每個清單各自最佳化重要，
  否則每一張卡都要重新學一次
- 飲食日記原本開 5 天：一天的餐點含照片就快占滿一個畫面，5 天會把日記下面的
  東西全部推出視線外
- **熱量標準值是唯一完全收合的**（連一筆都不開）。它不是紀錄，是設定值 ——
  拍照時默默在用，唯一會打開它的理由是要改或刪

### 加入行事曆（2026-09 新增）
- **純前端不能寫入手機行事曆**，沒有這個 API。但可以產生 `.ics` 交給系統，
  iPhone 點開就會跳「加入行事曆」並且欄位都填好了。不需要 Apple 配合、不需要後端
- 規則在 `lib/calendar.js`。iCalendar 難的不是格式，是那些「只在別人手機上壞掉」
  的細節，所以測試都針對這些：
  ① **折行要按 75 個位元組，不是 75 個字元。** 一個中文字三個位元組，
  照字元數折會把某個字切成兩半，事件標題就變成亂碼。所以折行是數 UTF-8 位元組、
  而且只在字元之間斷。測試會把折行還原回去比對原文，這才證明沒有切壞
  ② **全天事件的 DTEND 要填隔天。** 填同一天會變成長度為零，有些行事曆會直接丟掉
  ③ **UID 要穩定**（取自那筆排定的 id）。同一個檢查加兩次應該是更新，不是留下兩筆
- 提醒設在前一天（`TRIGGER:-P1D`）：當天早上已經來不及安排，一週前又會再忘一次
- 交付方式跟匯出備份一樣：先試手機的分享選單，桌機退回下載。
  **分享被取消（AbortError）時不能退回下載** —— 那會把她剛剛拒絕的檔案存進下載夾
- 排定的檢查和回診提醒放在同一張卡，因為對她來說是同一件事：
  有日期、還沒發生。**逾期一樣永遠不會因為太久而消失**

### 每一餐的影響（2026-09 新增）
- 拍照後除了熱量和燈號，會顯示「要留意的」和「對你有幫助的」，並**跟她的健檢報告
  比對**：命中她已經超出範圍的項目時會排在最前面並寫出她自己的數字
- **AI 只回傳「食材標籤」，所有健康影響的文字都寫在 `lib/nutritionTags.js`。**
  這是整個功能最重要的設計，不是實作細節：一個被要求評論食物對健康影響的視覺模型，
  會寫出流暢但像診斷的句子（「這會讓你血糖升高並導致糖尿病」），而純前端 App
  沒有任何機制能在它顯示前審查。所以模型的工作被限縮成「辨識盤子上有什麼」，
  回傳固定字彙；每一句她會讀到的話都在程式裡、都經過 `tools/test-nutrition.mjs` 檢查
- 文案三條規則（測試會擋）：
  ① **講負擔與機制，不講因果**。「增加血壓的負擔」可以，「會造成高血壓」不行 ——
  一餐不會造成一個疾病，這樣講是假的，而且是會讓人不再相信這個 App 的那種假
  ② **疾病只能寫成族群層級的關聯，不能寫成對她的預測**。
  「長期過量與心血管疾病風險有關」可以，「你會得心臟病」不行
  ③ **好的部分要跟壞的部分講得一樣清楚**。只會講食物哪裡不好的畫面，她會不想再打開。
  測試會檢查 burden 與 benefit 的數量大致平衡
- **每一個 burden 都必須附「改成什麼」**（`swap`）。沒有替代方案的提醒只是責備
- 標籤會存在飲食紀錄那一筆上（`entry.tags`），所以**照片過期被刪掉之後，
  這一餐吃了什麼負擔仍然查得到**
- 磷／鉀那個標籤刻意寫成「該限制多少要由醫師決定」，不給任何克數

### 熱量標準值（同一個食物記住她改過的數字）
- **問題**：包裝食品的熱量印在標示上、永遠不變（御選肉鬆飯糰就是 251），
  但 AI 每次都從外觀重新猜一次，她每次都要重改一次。改過的數字應該算數
- **規則在 `lib/foodMemory.js`，存在 `food-calories`（新的鍵，一天一食物一列）**
- **只有「她給的數字」才會被記住，AI 自己估的絕對不記。** 這是整個功能會幫忙
  還是會默默污染日記的分界：如果把 AI 的估算也記起來，一個猜測就會變成「標準
  值」，然後被自動套用到之後每一張照片，而畫面上沒有任何地方承認它本來只是猜的。
  三個入口都只在她真的動過數字時才記：拍照卡片裡改過（跟 `aiCalories` 比對）、
  日記那行輸入框真的觸發過 onChange（`editedCaloriesRef`）、手動輸入
- **名字只做「去空白、去標點」的正規化，不做模糊比對。**「白飯」和「白飯（大碗）」
  是兩個食物。把一個份量的熱量套到另一個份量上，正是這個功能該防止的錯
- **會直接套用，但一定看得見、一定可以一鍵退回。** 卡片上寫「已直接用你之前改的
  251 大卡。AI 這次估 320 大卡。」下面就是「這次改用 AI 估的 320 大卡」。
  一個自己變掉又不說的數字，比它取代掉的那個猜測更糟
- 手動輸入那邊是**提示不是自動填**：從頭打字通常代表是同名的另一份量
- **「我的熱量標準值」卡片可以看和刪。** 記錯的數字如果看不到也刪不掉，這個功能
  就變成一個會一直給錯數字的東西。刪掉之後下次重新讓 AI 估
- 備份／還原／重設都含這份資料；還原時會過同一道 `normalizeMemory` 修補，
  因為備份檔是使用者可以編輯的純文字，壞掉的數字會直接進到日記裡
- 測試在 `tools/test-food-memory.mjs`；拍照卡片的文案可以用
  `tools/preview-analysis.jsx` 看（那張卡要有 API key 才會出現，
  預覽頁不含 App 的樣式表，是用來看**字**的）

- **成長階段**：由**累計**達標天數推進（不是連續），一棵樹 **15** 個達標日，
  分八階：種子 0／發芽 1／幼芽 2／小苗 4／成長 6／成熟 9／破土 12／森林之樹 15。
  **本來是 30 天，2026-09 依使用者要求改成 15 天** —— 一個月才看得到一棵完成，
  對「撐下去的理由」來說太遠了。這個間距讓早期幾乎每達標一天畫面就會變，
  而且任何時候都不會超過三天沒有變化。
  **改 `TREE_DAYS` 會重算整座花園**：樹不是存起來的，是每次從達標歷史推算的
  （`gardenState`），所以砍半會讓已完成的樹變成兩倍。這裡是刻意的（同一份歷史、
  用比較短的週期去數），但也代表這個常數不能隨便動。
  斷一天不歸零，只是那天不往前走。階段名稱與順序來自使用者的設計稿，
  `lib/goals.js` 的 STAGES 和 Sprout.jsx 的圖必須一一對應（測試會擋）
- **花園**：每完成一棵樹就永久種進花園。花園狀態完全由每日摘要推算出來，
  沒有第二份資料會漂移
- **每日摘要**（`daily-summary`）：一天一筆、四個欄位，**永久保留不裁切**。
  因為飲食／喝水／運動紀錄分別只留 30／60／90 天，達標歷史必須比它們活得久
- **回填**：首次啟動時會從既有紀錄回推，但最多只能回推 30 天（判斷熱量達標要靠
  飲食紀錄）。更早的沒有資料可還原
- **植物不會枯死，而且連顏色都不變**：沒達標只有**表情**變睏，植物本身一模一樣。
  這樣「長到哪一階」和「今天過得怎樣」才是兩件分開讀得出來的事。
  絕對不要做成枯萎或轉褐 —— 這是給狀況不好的日子也會打開的 App
- **植物有臉，而且刻意跟喝水的水滴同一套語言**：`#3A4C3A` 的圓點眼睛、
  `#F4A6B7` 腮紅、`#F2C55C` 閃光、睏的時候眼睛變成閉合弧線（設計稿在種子和
  破土兩階就是這樣畫的）。四種表情剛好對應「今天達成幾項」（0／1／2／3）。
  第一版畫成植物學上正確但沒有表情的樹，結果像圖表而不像會想照顧的東西 ——
  **不要改回沒有臉的版本**。這些顏色是寫死的而不是跟主題變色，理由跟水滴永遠是
  藍色一樣：它是一個角色，不是一個介面表面
- **植物的圖是使用者的設計稿，不是我畫的。** 來源是她提供的
  「Health Forest 植物養成圖示系統」設計畫布，八張圖存在
  `source/tools/health-forest-stages.json`，由 `source/tools/gen-sprout.mjs`
  轉寫成 `components/Sprout.jsx`。**不要手改 Sprout.jsx，也不要自己重畫** ——
  要改外觀請改設計稿那份 JSON 再重跑產生器，讓 App 跟設計永遠是同一張圖。
  設計稿本身的規則：96×96 畫布（viewBox `-2 -2 100 100`）、
  所有描邊一律 `#3A4C3A` 粗 4.5、圓角端點、葉子只用同一個形狀靠旋轉縮放變化
- **產生器只做一件危險的事：把每一階固定的表情挖掉**，好讓 App 換上四種心情。
  這件事已經悄悄出錯過兩次，都不會報錯：
  ① 森林之樹的臉沒被挖掉（它畫在樹冠 y24，不像其他階畫在盆子 y79），
  結果樹冠一張臉、地上又多一張浮著的臉；
  ② 同一棵樹的**樹幹和它腳下的草丘被誤刪**，因為它們的起始高度剛好跟嘴巴同一帶，
  樹就浮在半空中。所以現在：臉的位置是**從每張圖自己讀出來的**（`FACE_AT`），
  不是全部釘在同一個高度；而且 `tools/test-sprout.mjs` 會拿設計稿逐個圖形比對，
  要求「設計稿畫的東西全都還在，只准少掉幾個落在記錄下來的臉部座標附近的小記號」。
  刪掉一根樹幹會被擋下來，留下一張臉也會
- **臉長在那個階段的主體上**：盆栽那七階在盆子上，森林之樹在樹冠上。
  這是設計稿自己的安排，`FACE_AT` 只是把它記下來。曾經做過「臉永遠在同一個位置」
  的版本，結果一棵樹底下掛著一張臉，看起來像兩隻生物而不是一隻長大
- **每一階跟地面接觸的高度不一樣**（`groundY(stage)`）：盆栽踩在盆底 y91，
  森林之樹踩在自己的草丘 y88。花園要把樹種在某個點上，用單一常數會讓樹浮起來
- **破土和森林之樹的圖裡本來就有閃光**，所以 `OWN_SPARKLES` 會擋掉 App 再加一層
- **改植物外觀時用 `source/tools/preview-sprout.jsx`**：一頁列出八階段 × 四表情，
  判斷「是不是同一隻生物在長大」只能一次看全部，一張一張截圖看不出來
- **插圖要小而精緻，不要大。** 植物被容器限高在 160px（`.growth-scene.is-sprout`），
  不是滿版。放大之後每個粗糙的地方都會被放大 —— 厚重的褐色土堆、粗線條的臉。
  現在土壤是柔和的接地陰影而非褐色色塊、五官線條變細、色塊加了細微明暗。
  花園（`.is-garden`）保留自己的長寬比，但地面用同樣的淡色處理，
  否則兩個檢視只差一次點擊卻像兩個 App
- **Sprout 與 Garden 的 svg 不寫死 inline 尺寸** —— inline style 會蓋掉所有
  stylesheet 規則，導致容器無法限制它。尺寸由放它的地方決定
- **八個階段共用同一個 viewBox**（`-2 -2 100 100`），這是設計稿的取景方式：
  每張圖自己就已經填滿畫布，所以階段推進時卡片高度不會跳動，也不會出現
  「一顆種子浮在大片空白裡」
- **三個每日條件各有一個小角色**（`components/GoalIcons.jsx`：飯碗、鞋子、水滴），
  取代單調的色點。臉就是狀態：未達標是圓點眼＋平嘴，達標變笑眼＋腮紅＋閃光。
  **角色的身體色比環的色票淺** —— 色票是圖表用色，墨色的臉壓在上面會看不見
  （`--move` 幾乎跟墨色同明度）。這跟水滴吉祥物用淺藍填色而非描邊色是同一個理由

## 健康分析（2026-09 新增）
這一頁把健檢報告接到每天的紀錄上：**本週重點 → 每月分析 → 報告本身 → 上傳**，
順序是照「能拿來做什麼」排的，不是照好不好算排的。

### 絕對不能違反的三條（`tools/test-plan.mjs` 會擋）
1. **只做比較，不做診斷。** 每個數值旁邊一定要寫出它被拿來比的區間
   （「糖尿病前期範圍 100-125」），畫面上呈現的是一個她可以自己核對的比較，
   不是一個要她相信的判決
2. **不預測任何數字會怎麼變。** 沒有人能保證「這樣做血糖就會降」，
   照著這種承諾去做的人，處境比被誠實告知的人更差。測試裡有禁用字清單
   （會降／就會／保證／治好／確診／停藥…）
3. **腎功能相關的發現完全不給飲食建議。** 蛋白質、鹽分的限制是醫師依整體狀況
   決定的，一個通用 App 亂猜會真的造成傷害。`kidney-refer` 這條規則沒有 measure、
   沒有飲食動作，只有「把報告帶去給醫師看」。肝指數同理

### 其他關鍵決定
- **照片讀完就丟，只留她確認過的數字**（`lib/reports.js`）。報告照片又大又是
  醫療文件，留一份在瀏覽器裡沒有任何好處
- **每個數值都要過「合理範圍」**（`plausible`，在 `lib/health.js` 的 LAB_MARKERS）。
  模型是從照片上讀數字，會掉小數點、會抄到旁邊那欄的「參考值」。
  空腹血糖 1080 不是一個嚇人的發現，是一次誤讀 —— 被擋掉並告訴她「這項請自己輸入」
- **沒量到 ≠ 正常。** `labNumber()` 存在的原因：`Number(null)` 是 0，而 0 落在好幾個
  項目的「正常」區間裡，所以原本沒填的欄位會被判成正常。這個 bug 只有測試抓得到
- **代謝症候群是報告和體態紀錄唯一交會的地方**：腰圍來自體態紀錄，其他四項來自報告，
  五項中三項是國健署公布的標準。舒張壓的判定值是 **85**（代謝症候群），
  不是血壓分區用的 80，兩者不能混用
- 五項沒量完時，「未達三項」不等於「你沒事」，畫面上一定要說清楚還有幾項沒量
- **每 7 天一輪，從報告日期起算**（`cycleFor`）。這樣「第 3 週」才有意義。
  進度只算到今天為止，不然目標 5 天在第 2 天就會顯示成沒做到
- **每月最後一天顯示總分析**，其他日子可以用月份選單回看。全部即時推算，不另存一份
- 舊月份的飲食／喝水／運動明細會過保留期（30／60／90 天），但每日摘要永久保留。
  這時要說「明細已超過保留期」，不能把缺資料顯示成一堆 0

## 運動建議與影片（2026-09 新增）
- **連結是 YouTube「搜尋」而不是特定影片。** 純前端的 App 沒辦法知道某支影片被刪了、
  變成私人了，而健康計畫裡一個點不開的連結比沒有連結更糟。搜尋網址永遠打得開、
  永遠是當下的結果，她也可以自己挑順眼的老師。**不要改成寫死 video id**，
  `tools/test-workouts.mjs` 會擋
- 她找到喜歡的影片可以釘在那個項目上（`workout-links`），之後就是一鍵。
  連結只檢查 http(s)（擋掉 `javascript:`），不限平台
- **建議是排序過的，不是清單。** 十二個項目條列出來只是型錄；四個各自附上
  「為什麼是你」才是建議。排序用健檢報告的異常項目、個人資料的病徵、BMI、
  以及最近一週實際記了多少運動
- **一定會有一項阻力、一項柔軟度。** 四支有氧影片不構成一週的運動，
  而且狀況差的日子只有最輕的那一項會真的發生
- BMI ≥ 27 只換掉項目（改成低衝擊），不減少數量
- 「從這一項開始最容易接上」只出現在第一張卡片。四張都寫就變成壁紙

## 每週運動目標的版面（2026-09 改版）
- **它曾經是一張四欄表格**（日／建議活動／時間／強度）。表格適合「橫向比較的資料」，
  不適合「希望你會想去做的一週」：裡面沒有任何東西會動、不知道你已經做了什麼，
  而且在手機上強度那一欄會斷成三行
- 現在是七張日卡（`components/WeeklyPlanCard.jsx`）：每天一個對應運動類型的小角色、
  強度用三顆點而不是括號說明、並且會顯示**這週實際做了沒有**
- **「還沒到」和「沒做到」不能長得一樣。** 未來的日子是淡的，不是打叉
- **當天做了別的運動要算成「有動」，不是漏掉。** 排伸展的那天騎了 40 分鐘的車
  不是失敗，畫成空圈既不準也讓人洩氣（`movedAnyway`）
- 週的起點是**星期一**（`weekStart`），因為模板是寫「週一…週日」。
  用「最近七天」會讓週一每天落在不同一列
- 三個角色在 `components/ExerciseIcons.jsx`，跟飯碗／鞋子／水滴同一套畫法
  （墨色圓點眼、腮紅、達成後加閃光）。**原本用 emoji（🏃🏋️🧘）**，
  每支手機長得不一樣，也跟 App 裡其他東西完全不搭

## 健檢報告一次可以選多張（2026-09 新增）
- 健檢報告很少只有一頁，所以相簿可以一次選最多 **10 張**（`MAX_PAGES`），
  合併成同一份報告。拍照那顆維持一次一張（相機本來就是）
- **一張一張讀，不要平行送。** Gemini 免費額度有速率限制，十個請求同時送會整批失敗，
  而依序送十個會成功。讀不出來的那張會被記下來，其餘繼續 ——
  五頁裡壞一頁不該連另外四頁一起丟掉
- **先讀到的值優先，後面的只補空缺。** 否則結果會取決於「照片剛好用什麼順序選的」
- **兩張讀到不同數字時，兩個都顯示、不要偷偷選一個。** 那代表其中一張讀錯了，
  而程式無法知道是哪一張。畫面上會寫「第 1 張是 108、第 3 張是 112（先採用 108）」
- 合併規則在 `lib/reports.js` 的 `mergeReadings`，測試在 `tools/test-labs.mjs`

## 頭像、稱謂與每日問候
- **頭像**：`components/AvatarPicker.jsx`。圓形、可換，跟 LINE 一樣。選到的圖會
  **中心裁切成正方形再縮到 200px**（`AVATAR_SIZE`）才存進 profile —— 它跟健康
  紀錄擠在同一個 localStorage 裡，直接塞手機原圖會有把整個儲存區撐爆、連體態
  紀錄一起壞掉的風險。實測一張直幅照片裁切後約 3 KB
- 沒設頭像時預設顯示豆苗角色，不是灰色人形剪影
- **稱謂**：`profile.nickname`，早晚的問候與總結都用它稱呼
- **每日正向語（真乘心語）／晚間總結**：`lib/coach.js` ＋
  `components/DailyCoach.jsx`。**一定要有一張卡在畫面上**：晚上 7 點前是
  「真乘心語」，7 點後換成總結。曾經做成「11 點後就不顯示、晚上 7 點才回來」，
  結果整個白天什麼都看不到 —— 使用者以為這個功能沒做。
  **一個大部分時間看不到的功能等於沒做。**
  11 點前會多一行「早安，{稱謂}」，之後只留標題。
  可以關掉，關掉的狀態記在 `coach-dismissed`
- **真乘心語的句子是使用者自己的文件，不是 App 的文案。** 存在
  `lib/quotes.js`，由 `tools/import-quotes.mjs` 從她的 .docx 產生
  （文件裡每個 `*` 是一句的開頭）。**不要改寫、不要修標點、不要套用
  coach.js 的語氣規則** —— 那些規則是用來管 App 自己寫的字。
  要更新請重跑匯入器而不是手改這個檔，讓它保持是原文的忠實副本。
  只收 12–36 字的句子（手機卡片兩行內讀完）；更長的在原文裡是段落不是格言。
  原文若有破損（括號沒閉合）會被排除並在匯入時列出來，不會私自修改她的字
- **文案有三條規則，`tools/test-coach.mjs` 會擋**：只講行為不評判人、
  不做任何醫療宣稱、**永遠先講一件做對的事再講不足**。連三項全沒達成的那天
  也要有東西可以講（那天做對的事就是「有打開來看」）。測試裡有一份禁用字
  黑名單（失敗／不夠／退步／血糖會…）
- 早晨問候由日期決定，所以**同一天整個早上都是同一句** —— 每次打開都換一句
  會變成雜訊而不是問候

## 核心功能（目前已完成，供你了解全貌）
- **個人資料**：年齡/性別/身高/體重/主要病徵；平時活動量改成「強度/中度/輕度」三種
  勾選＋每週分鐘數，App 依 WHO/ADA 準則自動換算成 sedentary/light/active 供內部計算用
- **飲食建議**：紅綠燈飲食指南；AI 拍照熱量分析（Gemini 免費／Anthropic 付費可切換）；
  手動輸入；飲食紀錄（最近7天，可直接編輯已存的熱量數字，含照片縮圖）；官方食品營養
  成分資料庫查詢連結
- **今日熱量目標**：優先用「體態紀錄」裡最新一筆的基礎代謝率(BMR)計算，沒有的話用
  年齡/性別/身高/體重公式估算；使用者也可以手動輸入自訂目標覆寫系統計算值；有展開
  區塊說明計算方式
- **喝水量**：依體重/性別/活動量估算目標；一鍵快速記錄常見份量；歷史紀錄以「每日
  總量」保存（不是每次點擊都存一筆）；可愛水滴角色依達成率變換表情；有趨勢圖
- **運動建議**：每週運動計畫模板；運動紀錄與達成率（依強度分類為有氧/阻力/柔軟度，
  跟每週150分鐘目標比較，給出建議文字）；運動項目含快走以外的多種選項
- **體態紀錄**：體重/BMI/腰圍/體脂肪/內臟脂肪/骨骼肌率/體年齡/BMR/**睡眠時數**；
  睡眠有自己的趨勢圖與參考區間（`sleepZones`，7-9 小時為建議範圍）。
  **睡眠刻意不是第四個每日達標項** —— 達標的三項是花園在數的東西，偷偷改變
  「達標」的定義會把已經記錄的每一天的意義都改掉；BMI、體脂肪率、
  骨骼肌率、腰圍的趨勢圖都有依衛福部/醫療常見標準做顏色分區背景（一眼看出好壞）；
  歷史紀錄預設只顯示最新3天，超過會有「展開全部歷史紀錄」按鈕依月份分組顯示
- **資料備份**：匯出/匯入 JSON，匯出優先跳出手機分享選單（可存 Google Drive/iCloud），
  裝置不支援分享 API 時自動退回直接下載

## 重要架構決策（改的時候要注意）
- **App 名稱是 Healthy Care（2026-09 從「糖前哨」改名）。改名只動顯示字串，
  絕對不要動 localStorage 的鍵名** —— `profile`、`body-records`、`food-log`、
  `water-log`、`exercise-log`、`daily-summary`、`food-calories`、`health-reports`、
  `workout-links`、`clinic-visits`、`exam-plans` 這些是找到既有資料的唯一途徑，
  改了等於把手機上所有紀錄變成孤兒。改名是換標籤，不是資料遷移。
  Service Worker 的 `CACHE_NAME` 則要跟著換版，否則舊快取會讓改名看不出來
- **`lib/health.js` 已經手改過**（`sleepZones` 與對應的 CONTENT_REVIEW 條目），
  所以 `tools/extract-health.mjs` 現在會拒絕覆蓋它。要改健康數值請直接改
  `lib/health.js`，不要重跑抽取器
- **在 lib/ 新增 export 後記得加進 `export { }` 清單** —— 忘記加的話 esbuild
  會直接報 "No matching export"（這點它擋得住，跟忘記 import 不同）
- **資料欄位名不能猜。** 讀錯欄位不會報錯，只會回傳 0，然後某個判定就永遠不成立。
  這已經真的發生過一次：運動時間存的是 `durationMin`，但判定邏輯讀 `minutes`，
  結果運動這項永遠無法達標，而測試因為用了同樣錯的名字所以全過。三個欄位名集中在
  `lib/goals.js` 的 `FIELDS`，測試裡有直接斷言。新增讀取紀錄的程式碼一律用 `FIELDS`
- **`lib/health.js` 是健康數值的唯一來源。** 不要把裡面的常數或公式複製到別的檔案 ——
  兩份副本遲早會各自漂移，而漂移的那份不會報錯，只會給出錯的健康建議
- **飲食紀錄的照片會過期，文字不會。** 滿 30 天（`PHOTO_DAYS`）只刪照片、保留文字，
  這樣日記才有長期歷史。照片一張約 20KB，一年三餐就會超過瀏覽器 localStorage 的
  容量上限；文字一天約 300 bytes，十年才 1MB。**不要改成連文字一起刪**
- **彈出視窗一定要有高度上限和捲動，而且按鈕要釘住。** 飲食分析卡片加上健康
  影響說明之後就超出畫面，結果「加入紀錄」被推到螢幕外 —— **看得到卻存不了，
  比不顯示那些說明更糟**。現在 `.analysis-modal-card` 是 `max-height:88dvh` 的
  直向 flex、內容區自己捲動、`.analysis-actions` 用 sticky 釘在捲動區底部。
  往卡片裡加東西之前，先確認它有沒有高度上限
- **版面在 320px 一定要能塞得下，不能只測 375px。** 使用者回報「一打開總覽
  就要手動縮圖」，原因是整個頁面在 320px 寬的螢幕上被撐成 **397px**。
  iPhone 開了「顯示縮放（放大文字）」之後回報的就是 320px 邏輯寬度，
  所以這不是舊機型的邊角情況。兩個成因都很隱形：
  ① **曾經有兩層 `.app-shell` 疊在一起**（外層只是為了包 `<style>`），
  內層作為 flex 子項被算成 min-content 而不是螢幕寬度。現在外層改成 fragment，
  並且 `.app-shell` 加上 `width:100%; min-width:0` 當安全網
  ② **三環那一列（`.ring-row`）縮不下去** —— 圖示和標籤是固定寬、目標與差額都是
  `white-space:nowrap`，整列硬底線約 290px。現在改成三欄 grid
  （角色／讀數／結論），讀數自己內部換行，結論永遠靠右。
  **不要改回可以 wrap 的單層 flex** —— 那樣「達標」會被擠到獨立一行看起來像壞掉
- **改版面後請用 320px 量一次**：把每個元素跟它父層的 clientWidth 比對，
  找出「比父層寬」的那一個，比看截圖快也可靠得多（預覽窗的截圖經常是空白的）
- **iOS 的 `input[type=date]` 會無視 `width:100%`。** 它是原生控制項，Safari 依
  自己的內容決定寬度，結果「體態紀錄 → 日期」欄位整條撐出卡片外（使用者回報的
  「超線」）。桌機瀏覽器看起來完全正常，所以這種問題只在她的手機上出現。
  解法在 CSS：date／time 欄位加 `-webkit-appearance:none` 讓它照一般輸入框排版。
  另外 `.field-row` 是 grid，子項預設 `min-width:auto` 不會縮到比內容窄，
  所以也補了 `min-width:0` —— 兩欄式的列以後放寬欄位才不會又擠出去
- **定性項目跟數值存在同一個 `health-reports` 裡**（`report.flags`），
  不要另開一個鍵 —— 它們是同一份報告的同一頁
- **匯出的欄位清單只有一份（`BACKUP_FIELDS`，在 `lib/storage.js`）。**
  匯出畫面是用 React state 組檔案、不是重讀 storage，所以曾經兩邊各寫各的，
  結果「熱量標準值」加進了 restore 和 `buildBackup()`，**實際下載到的檔案裡卻沒有**。
  備份只有在出事那天才有價值，而那天已經來不及發現少了一欄。
  `tools/test-backup.mjs` 會去讀 `restoreBackup` 的原始碼，
  要求「restore 讀得到的欄位，export 一定寫得出來」
- **不要**把資料存取改回 `window.storage` 直接呼叫官方 Claude.ai 環境的版本；這個獨立
  版本靠 `storage-shim.js` 模擬同樣的 get/set/delete/list 介面存進 localStorage，維持
  這層抽象可以讓程式碼其餘部分不用大改
- 所有 `setXxxForm({...form, field: value})` 這種直接展開更新的 pattern，如果同一個表單
  裡有 checkbox/chip 點擊跟文字輸入同時存在，容易觸發 React 批次更新的競態問題（已經修過
  一次真實案例：exerciseForm 的 chip 選擇被緊接著的文字輸入蓋掉）。新增表單欄位時優先用
  `setForm((f) => ({...f, field: value}))` 函式型寫法，比較安全
- AI 拍照分析的 prompt 裡有特別要求：如果照片拍到包裝食品的營養標示文字，要優先讀取
  標示上的數字，而不是純外觀估算；也要求 AI 誠實回報 confidence，不要灌水
- 健康相關的參考數值（BMI/體脂肪/骨骼肌率/腰圍分區、飲水量公式、熱量計算公式）都盡量
  引用衛福部/國健署等官方或準官方來源，並記錄在 `CONTENT_REVIEW` 這個常數裡（含
  `lastReviewed` 校對日期與 `sources` 來源清單），之後調整這類數值記得一併更新它
- 曾經有一個「網址參數自動記錄」的功能（給 iOS 捷徑/Apple 健康自動化用）因為有資料
  被意外覆蓋的風險而被移除了；除非使用者明確要求，不要重新加類似「打開頁面就自動
  寫入資料」的機制
- **Apple Watch／Apple 健康連動做不到，不要再評估。** HealthKit 只開放給原生 iOS
  App，網頁沒有這個 API。要做到必須整個改寫成原生 App（Mac＋Xcode＋開發者帳號年費），
  而且三項裡只有運動分鐘能自動，不值得
- **但「每天手動輸入」這個真正的痛點是可以解的，而且不需要 Apple 配合：拍照。**
  使用者問的其實不是「要不要接 HealthKit」，是「量完 OMRON 之後能不能不要打八個
  數字」。所以體態紀錄多了「拍體重計自動填入」（`lib/bodyScan.js` ＋
  `BODY_PROMPT`）：拍體重計的顯示螢幕，或 OMRON connect／Apple 健康的截圖都行。
  **這條路任何體重計都適用、不綁 Apple、也不需要後端**，是這個架構下最好的解
- 讀出來的數字**填進表單、不直接存檔**：它是草稿，直到她按儲存為止，跟自己打的
  一樣可以改。照片同樣不留
- **照片沒讀到的欄位絕對不能被清空。** 體重計不量腰圍，不該把她三十秒前打的腰圍
  洗掉（`applyReadingToForm` 只寫有值的欄位，測試有擋）
- 每個值一樣要過合理範圍（`BODY_FIELDS[].plausible`）：七段顯示器會被讀成
  62.4 → 624，而 624% 體脂肪不是發現，是誤讀。被擋掉的會告訴她「這項請自己輸入」
- **手機主動跳的推播提醒也做不到。** 純前端靜態網站沒有後端可以推送。提醒的實際做法
  是「傍晚打開 App 時，若還差一項就顯示」，搭配補登前一天（`canBackfill`）

## 部署方式
GitHub Pages：repo 設定為 Public，Settings → Pages → Build and deployment →
Source 選「Deploy from a branch」→ Branch 選 main、資料夾選 `/ (root)`。
上傳/push 新的 `app.bundle.js` 等檔案後，GitHub Pages 會在幾分鐘內自動重新部署。

## 使用者背景（互動時可以注意的地方）
使用者不是工程背景，之前在這個專案的開發過程中，Shortcuts、GitHub 網頁上傳、
Service Worker 快取等技術性步驟都需要很詳細、逐步的圖解式說明才能順利完成。
說明時盡量白話、步驟化，避免預設對方熟悉開發者工具或術語。
